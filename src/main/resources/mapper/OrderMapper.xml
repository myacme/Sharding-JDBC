<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.shardingjdbc.mapper.OrderMapper">

    <!-- 结果映射 -->
    <resultMap id="BaseResultMap" type="com.example.shardingjdbc.entity.Order">
        <id column="order_id" property="orderId" jdbcType="BIGINT"/>
        <result column="user_id" property="userId" jdbcType="BIGINT"/>
        <result column="order_no" property="orderNo" jdbcType="VARCHAR"/>
        <result column="product_name" property="productName" jdbcType="VARCHAR"/>
        <result column="quantity" property="quantity" jdbcType="INTEGER"/>
        <result column="amount" property="amount" jdbcType="DECIMAL"/>
        <result column="status" property="status" jdbcType="INTEGER"/>
        <result column="create_time" property="createTime" jdbcType="TIMESTAMP"/>
        <result column="update_time" property="updateTime" jdbcType="TIMESTAMP"/>
        <result column="remark" property="remark" jdbcType="VARCHAR"/>
    </resultMap>

    <!-- 基础字段 -->
    <sql id="Base_Column_List">
        order_id, user_id, order_no, product_name, quantity, amount, status, create_time, update_time, remark
    </sql>

    <!-- 插入订单 -->
    <insert id="insert" parameterType="com.example.shardingjdbc.entity.Order" >
        INSERT INTO t_order (order_id, user_id, order_no, product_name, quantity, amount, status, create_time, update_time, remark)
        VALUES (#{orderId}, #{userId}, #{orderNo}, #{productName}, #{quantity}, #{amount}, #{status}, #{createTime}, #{updateTime}, #{remark})
    </insert>

    <!-- 根据订单ID查询订单 -->
    <select id="selectById" parameterType="java.lang.Long" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM t_order
        WHERE order_id = #{orderId}
    </select>

    <!-- 根据用户ID查询订单列表 -->
    <select id="selectByUserId" parameterType="java.lang.Long" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM t_order
        WHERE user_id = #{userId}
        ORDER BY create_time DESC
    </select>

    <!-- 根据订单号查询订单 -->
    <select id="selectByOrderNo" parameterType="java.lang.String" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM t_order
        WHERE order_no = #{orderNo}
    </select>

    <!-- 查询所有订单 -->
    <select id="selectAll" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM t_order
        ORDER BY create_time DESC
    </select>

    <!-- 更新订单信息 -->
    <update id="update" parameterType="com.example.shardingjdbc.entity.Order">
        UPDATE t_order
        <set>
            <if test="orderNo != null">order_no = #{orderNo},</if>
            <if test="productName != null">product_name = #{productName},</if>
            <if test="quantity != null">quantity = #{quantity},</if>
            <if test="amount != null">amount = #{amount},</if>
            <if test="status != null">status = #{status},</if>
            <if test="updateTime != null">update_time = #{updateTime},</if>
            <if test="remark != null">remark = #{remark},</if>
        </set>
        WHERE order_id = #{orderId}
    </update>

    <!-- 根据订单ID删除订单 -->
    <delete id="deleteById" parameterType="java.lang.Long">
        DELETE FROM t_order WHERE order_id = #{orderId}
    </delete>

    <!-- 分页查询订单 -->
    <select id="selectByPage" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM t_order
        ORDER BY create_time DESC
        LIMIT #{offset}, #{limit}
    </select>

    <!-- 查询订单总数 -->
    <select id="countAll" resultType="java.lang.Integer">
        SELECT COUNT(*) FROM t_order
    </select>

    <!-- 根据用户ID分页查询订单 -->
    <select id="selectByUserIdAndPage" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM t_order
        WHERE user_id = #{userId}
        ORDER BY create_time DESC
        LIMIT #{offset}, #{limit}
    </select>

    <!-- 根据用户ID查询订单总数 -->
    <select id="countByUserId" parameterType="java.lang.Long" resultType="java.lang.Integer">
        SELECT COUNT(*) FROM t_order WHERE user_id = #{userId}
    </select>

</mapper>
